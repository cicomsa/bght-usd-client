export const formStyle = theme => ({
  container: {
    marginRight: 20,
    width: 200
  },
  button: {
    marginBottom: 7
  }
});

export const tablesStyle = theme => ({
  root: {
    width: '100%',
    marginTop: theme.spacing.unit * 3,
    overflowX: 'auto',
    display: 'flex'
  },
  table: {
    maxWidth: 300
  },
  checkIcon: {
    margin: 20
  }
});
